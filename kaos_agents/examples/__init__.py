"""Runnable examples shipped with kaos-agents.

Each subpackage is a self-contained, copy-paste-ready demonstration of
a real value prop. Fixtures live alongside the script and are reachable
from any install (editable or wheel) via :mod:`importlib.resources`.
"""
